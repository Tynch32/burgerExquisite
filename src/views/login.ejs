<!DOCTYPE html>
<html lang="en">
    <%- include('partials/head', {title: "Ingresar!"}) %>


<body class="detalle">
    <%- include('partials/header') %>

    <main id="home_main" class="home_main">
        <div class="row">
            <div class="col-12 col-md-10 col-lg-8 col-sm-11 mx-auto mt-5">
                <div id="formulario_login" class="card mb-3 shadow-lg">
                  <div class="card-header">
                    Ingresá
                  </div>
                  <div class="card-body">
                    <form action="" method="POST" class="row">
                        <div class="col-12 col-md-6 mb-3">
                            <label for="email" class="form-label">Email</label>
                            <input type="email" class="form-control" id="email" placeholder="Tu email" name="email"
                            value="<%= locals.old && (old.email && old.email ) %>">
                            <small hidden id="login_form_email" class="text-danger ms-2">
                              * El email es obligatorio
                            </small>
                            <small id="login_form_email_error" class="text-danger ms-2">
                              <%= locals.errors && (errors.email && errors.email.msg) %>
                            </small>
                        </div>
                        <div class="col-12 col-md-6 mb-3">
                            <label for="password" class="form-label">Contraseña</label>
                            <input type="password" class="form-control" id="password" placeholder="Tu contraseña" name="password">
                            <small hidden id="login_form_password" class="text-danger ms-2">
                              * La contraseña es obligatoria
                            </small>
                            <small id="login_form_password_error" class="text-danger ms-2">
                              <%= locals.errors && (errors.password && errors.password.msg) %>
                            </small>
                        </div>
                        <div class="col-12 col-md-6 mb-3">
                          ¿Aún no estás registrado?
                          <a class="login_to_register btn btn-secondary" href="/users/register">
                             ¡Regístrate aquí!
                          </a>
                        </div>
                        <div class="d-flex justify-content-between">
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" value="" id="remember" name="remember">
                                <label class="form-check-label" for="remember">
                                  Recordarme
                                </label>
                            </div>
                            <button disabled id="button_login_ingresar" type="submit" class="btn btn-dark">Ingresá <i class="fa-solid fa-right-to-bracket"></i></button>
                        </div>
                    </form>
                  </div>
                </div>
            </div>
        </div>
    </main>
    <%- include('partials/footer') %>
    <%- include('partials/scripts') %>
    <script src="/js/loginValidatorFront.js"></script>
</body>

</html>