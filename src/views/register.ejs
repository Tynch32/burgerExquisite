<!DOCTYPE html>
<html lang="en">
    <%-include('partials/head',{title: "Registrate"}) %>
<body class="detalle">
    <%-include('partials/header',{title: "Registrate"})%>
    <main class="home_main">
        <div class="row">
            <div class="col-12 col-md-10 col-lg-8 col-sm-11 mx-auto mt-5">
                <div id="form_register_box" class="card mb-3 shadow-lg">
                  <div class="card-header">
                    <b>Registro de usuario</b>
                    <small id="form_register_asteriscos">
                        Los campos con un * son obligatorios
                    </small>
                  </div>
                  <div class="card-body">
                    <form action="" method="POST" class="row">
                        <br>
                        <div class="col-12 col-md-6 mb-3">
                            <label for="name" class="form-label">Nombre </label><small id="form_register_asterisco_name">
                                *
                            </small>
                            <input type="text" class="form-control" id="name" placeholder="Ingrese su nombre" name="name"
                            value="<%= locals.old && (old.name && old.name )%>">
                            <small hidden id="register_form_name" class="text-danger ms-2">
                                * El nombre es obligatorio
                            </small>
                            <small id="name_error" class="text-danger ms-2">
                                <%= locals.errors && (errors.name && errors.name.msg) %>
                            </small>
                        </div>
                        <div class="col-12 col-md-6 mb-3">
                            <label for="surname" class="form-label">Apellido</label><small id="form_register_asterisco_surname">
                                *
                            </small>
                            <input type="text" class="form-control" id="surname" placeholder="Ingrese su apellido" name="surname"
                            value="<%= locals.old && (old.surname && old.surname ) %>">
                            <small hidden id="register_form_surname" class="text-danger ms-2">
                                * El apellido es obligatorio
                            </small>
                            <small id="surname_error" class="text-danger ms-2">
                                <%= locals.errors && (errors.surname && errors.surname.msg) %>
                            </small>
                        </div>
                        <div class="col-12 col-md-6 mb-3">
                            <label for="dni" class="form-label">DNI *</label>
                            <input type="number" class="form-control" id="dni" placeholder="Ingrese su DNI" name="dni"
                            value="<%= locals.old && (old.dni && old.dni ) %>">
                        </div>
                        <div class="col-12 col-md-6 mb-3">
                            <label for="email" class="form-label">Email</label><small id="form_register_asterisco_email">
                                *
                            </small>
                            <input type="email" class="form-control" id="email" placeholder="Tu email" name="email"
                            value="<%= locals.old && (old.email && old.email ) %>">
                            <small hidden id="register_form_email" class="text-danger ms-2">
                                * El email es obligatorio
                            </small>
                            <small id="email_error" class="text-danger ms-2">
                                <%= locals.errors && (errors.email && errors.email.msg) %>
                            </small>
                        </div>
                        <div class="col-12 col-md-6 mb-3">
                            <label for="password" class="form-label">Contraseña</label><small id="form_register_asterisco_password">
                                *
                            </small>
                            <input type="password" class="form-control" id="password" placeholder="Tu contraseña" name="password">
                            <small id="register_form_password" class="text-danger ms-2">
                                <p id="password_validation_1">* Debe contener al menos 8 caracteres</p>
                                <p id="password_validation_2">* Debe contener al menos 1 mayúscula</p>
                                <p id="password_validation_3">* Debe contener al menos 1 minúscula</p>
                                <p id="password_validation_4">* Debe contener al menos 1 número</p>
                                <p id="password_validation_5">* Debe contener al menos 1 carácter especial</p>
                            </small>
                            <small id="password_error" class="text-danger ms-2">
                                <%= locals.errors && (errors.password && errors.password.msg) %>
                                <%= !locals.errors ?"":("Debes volver a escribir la contraseña") %>
                            </small>
                        </div>
                        <div class="col-12 col-md-6 mb-3">
                            <label for="password2" class="form-label">Confirma contraseña</label><small id="form_register_asterisco_password2">
                                *
                            </small>
                            <input type="password" class="form-control" id="password2" placeholder="Confirma tu contraseña" name="password2">
                            <small hidden id="register_form_password2" class="text-danger ms-2">
                                * Debe confirmar su contraseña
                            </small>
                            <small id="password2_error" class="text-danger ms-2">
                                <%= locals.errors && (errors.password2 && errors.password2.msg) %>
                                <%= !locals.errors ?"":("Debes volver a escribir la contraseña") %>
                            </small>
                        </div>
                        <div class="d-flex justify-content-end">
                            <a href="/users/login" class="btn btn-secondary">Ya tengo cuenta!</a>
                            &nbsp;
                            <button  id="button_register_success" type="submit" class="btn btn-dark">Registrate</button>
                        </div>
                    </form>
                  </div>
                </div>
            </div>
        </div>
    </main>
    <%- include('partials/footer') %>
    <%- include('partials/scripts') %>
    <script src="/js/registerValidatorFront.js"></script>
</body>

</html>